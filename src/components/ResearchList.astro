---
import ResearchItem from './ResearchItem.astro';
const { label, items } = Astro.props;
---
<div class="shead"><span class="slabel">{label}</span></div>
<section>
  {items.map((it: any) => <ResearchItem {...it} />)}
</section>
